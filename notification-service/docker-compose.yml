# notification-service/docker-compose.yml
services:
  notification-service:
    container_name: notification-service
    build: .
    image: notification-service:latest
    env_file:
      - .env.staging
    environment:
      NODE_ENV: ${NODE_ENV:-staging}
      PORT: ${PORT:-3012}
      APP_RESOURCE_NAME: contact
      # força a URL correta dentro da rede Docker
      JWT_SERVICE_URL: https://auth-service:3000/v1
    ports:
      - "${PORT:-3012}:${PORT:-3012}"
    networks:
      smart-net:
        aliases: [notification-service]
    healthcheck:
      test: ["CMD-SHELL","curl -ks https://localhost:${PORT:-3012}/health || curl -s http://localhost:${PORT:-3012}/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 10
    volumes:
      - ./certs:/app/certs:ro
      - ./logs:/app/logs
networks:
  smart-net:
    external: true
