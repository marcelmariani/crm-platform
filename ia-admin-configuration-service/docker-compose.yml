# ia-admin-configuration-service/docker-compose.yml
services:
  ia-admin-configuration-service:
    container_name: ia-admin-configuration-service
    build: .
    image: ia-admin-configuration-service:latest
    env_file:
      - .env.staging
    environment:
      NODE_ENV: ${NODE_ENV:-staging}
      PORT: ${PORT:-3013}
      #APP_RESOURCE_NAME: bank-correspondent
      # força a URL correta dentro da rede Docker
      JWT_SERVICE_URL: https://auth-service:3000/v1
    ports:
      - "${PORT:-3013}:${PORT:-3013}"
    networks:
      smart-net:
        aliases: [ia-admin-configuration-service]
    healthcheck:
      test: ["CMD-SHELL","curl -ks https://localhost:${PORT:-3013}/health || curl -s http://localhost:${PORT:-3013}/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 10
    volumes:
      - ./certs:/app/certs:ro
      - ./logs:/app/logs
networks:
  smart-net:
    external: true
